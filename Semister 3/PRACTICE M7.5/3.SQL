-- GET MAX SALARY EMPLOYEE NAME, JOB TITLE
SELECT EMPLOYEES.FIRST_NAME, JOBS.JOB_TITLE
FROM EMPLOYEES 
JOIN JOBS
WHERE EMPLOYEES.JOB_ID = JOBS.JOB_ID AND EMPLOYEES.SALARY = (
	SELECT MAX(SALARY)
    FROM EMPLOYEES
);