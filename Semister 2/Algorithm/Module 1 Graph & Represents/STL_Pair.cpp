#include <bits/stdc++.h>
using namespace std;

int main()
{
    // make pair with two diffrent diffrent data type and simillar also.
    pair<string, int> p;
    p = make_pair("Sujoy", 20);
    cout << p.first << " " << p.second;

    return 0;
}