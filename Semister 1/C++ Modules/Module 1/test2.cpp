#include <bits/stdc++.h>
using namespace std;

int main()
{
    // welcome to the program sp, let's do some hack;
    int a=40, b=30;
    swap(a, b);

    // cout << a << " " << b;
    cout << min(a, b);
}