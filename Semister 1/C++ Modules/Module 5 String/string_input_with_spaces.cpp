#include <bits/stdc++.h>
using namespace std;

int main()
{
    // welcome to the program sp, let's do some hack;
    int a;
    cin >> a;
    string text;
    // cin >> text; // without spaces
    getline(cin, text); // with spaces;
    cout << text << endl;

    return 0;
}