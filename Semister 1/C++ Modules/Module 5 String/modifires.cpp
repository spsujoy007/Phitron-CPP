#include <bits/stdc++.h>
using namespace std;

int main()
{
    // welcome to the program sp, let's do some hack;
    string a = "Hello";
    string b = "world";
    // b = "Bd";
    // a += b;
    // a.append(b);
    // b[4] = 'b';
    // b.push_back('a');
    // b.pop_back();
    // cout << a << endl;
    // a.replace(5, 0, "_");
    a.insert(5, "bbb");
    cout << a << endl;

    return 0;
}